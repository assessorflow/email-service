spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  cloud:
    gcp:
      pubsub:
        enabled: false

aws:
  region: ap-southeast-1
  credentials:
    access-key: test-key
    secret-key: test-secret
  ses:
    from-email: test@assessorflow.com
