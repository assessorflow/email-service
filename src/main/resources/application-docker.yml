spring:
  datasource:
    url: jdbc:mysql://mysql:3306/email_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    show-sql: false

logging:
  level:
    sg.edu.nus.iss.email: INFO
