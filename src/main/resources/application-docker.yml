spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/email_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false

logging:
  level:
    sg.edu.nus.iss.email: INFO
