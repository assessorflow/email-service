spring:
  cloud:
    gcp:
      project-id: accessorflow
  datasource:
    url: jdbc:mysql://localhost:3306/email_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  jpa:
    show-sql: true

logging:
  level:
    sg.edu.nus.iss.email: DEBUG
